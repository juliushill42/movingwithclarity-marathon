# Build tapes

Filename contract: T21-001.webm

```
ffmpeg -i capture.mov -c:v libvpx-vp9 -crf 32 -b:v 0 -c:a libopus T21-001.webm
```

No private family recordings, credentials, or held claim walkthroughs.
Empty slot = no tape yet.
